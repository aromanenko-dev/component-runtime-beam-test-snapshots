name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build -e BEAM_VERSION -t components-runtime-beam-snapshots-test .
      env:
        BEAM_VERSION: 2.19.0-SNAPSHOT
